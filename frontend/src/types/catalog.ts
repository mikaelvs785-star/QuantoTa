export type Product = { id: string; name: string; category: string; market: string; price: number; previousPrice?: number; image: string };
export type Category = { name: string; icon: string; color: string };
