import { Search, SlidersHorizontal } from "lucide-react";
import { Input } from "@/components/ui/Input";
import type { ProductSort, ProductStatus } from "@/types/product";
type Props = { search: string; category: string; status: ProductStatus | "ALL"; sort: ProductSort; onChange: (filters: { search?: string; category?: string; status?: ProductStatus | "ALL"; sort?: ProductSort }) => void; };
const selectClass = "h-11 rounded-xl border bg-white px-3 text-sm outline-none focus:border-brand-500 focus:ring-3 focus:ring-brand-100 dark:bg-slate-900 dark:focus:ring-brand-500/20";
export function ProductFilters({ search, category, status, sort, onChange }: Props) { return <div className="grid gap-3 rounded-2xl border bg-white p-4 shadow-sm dark:bg-slate-900 md:grid-cols-[1fr_auto_auto_auto]"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><Input value={search} onChange={(event) => onChange({ search: event.target.value })} className="pl-10" placeholder="Pesquisar produtos" aria-label="Pesquisar produtos" /></div><select className={selectClass} value={category} aria-label="Filtrar por categoria" onChange={(event) => onChange({ category: event.target.value })}><option value="">Todas as categorias</option><option value="Mercearia">Mercearia</option><option value="Hortifruti">Hortifruti</option><option value="Laticínios">Laticínios</option><option value="Limpeza">Limpeza</option></select><select className={selectClass} value={status} aria-label="Filtrar por status" onChange={(event) => onChange({ status: event.target.value as ProductStatus | "ALL" })}><option value="ALL">Todos os status</option><option value="ACTIVE">Ativos</option><option value="INACTIVE">Inativos</option></select><label className="relative"><SlidersHorizontal className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><select className={`${selectClass} w-full pl-9`} value={sort} aria-label="Ordenar produtos" onChange={(event) => onChange({ sort: event.target.value as ProductSort })}><option value="newest">Mais recente</option><option value="oldest">Mais antigo</option><option value="az">A-Z</option><option value="za">Z-A</option></select></label></div>; }
