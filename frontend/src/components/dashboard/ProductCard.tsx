import { TrendingDown } from "lucide-react";
import { DashboardCard } from "./DashboardCard";
import { formatCurrency } from "@/lib/utils";
import type { MonitoredProduct } from "@/types/dashboard";
export function ProductCard({ product }: { product: MonitoredProduct }) { return <DashboardCard><div className="flex gap-3"><span className="grid size-12 place-items-center rounded-xl bg-slate-100 text-2xl dark:bg-slate-800">{product.imageUrl ? <img src={product.imageUrl} alt="" className="size-full rounded-xl object-cover" /> : "🛒"}</span><div className="min-w-0"><h3 className="truncate font-bold">{product.name}</h3><p className="mt-1 text-xs text-slate-500">Monitorado</p></div></div><div className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><p className="text-xs text-slate-500">Menor preço</p><p className="mt-1 font-bold text-emerald-600">{formatCurrency(product.lowestPrice)}</p></div><div><p className="text-xs text-slate-500">Maior preço</p><p className="mt-1 font-bold">{formatCurrency(product.highestPrice)}</p></div></div><p className="mt-4 flex items-center gap-1 text-sm font-bold text-emerald-600"><TrendingDown className="size-4" /> Economia de {formatCurrency(product.savings)}</p></DashboardCard>; }
