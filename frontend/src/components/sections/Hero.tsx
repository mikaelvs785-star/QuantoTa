import { ArrowRight, MapPin } from "lucide-react";
import { motion } from "framer-motion";
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/Button";
import { SearchBar } from "@/components/ui/SearchBar";
export function Hero() { return <section className="overflow-hidden bg-gradient-to-br from-brand-50 via-white to-amber-50 py-14 dark:from-brand-500/10 dark:via-slate-950 dark:to-amber-500/5 sm:py-20"><div className="mx-auto grid max-w-7xl items-center gap-10 px-4 sm:px-6 lg:grid-cols-[1.1fr_.9fr] lg:px-8"><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .45 }}><span className="inline-flex items-center gap-1 rounded-full bg-brand-100 px-3 py-1 text-sm font-bold text-brand-700 dark:bg-brand-500/15 dark:text-brand-100"><MapPin className="size-4" /> Compare preços reais</span><h1 className="mt-5 max-w-2xl text-4xl font-black leading-tight tracking-tight sm:text-5xl">Encontre o melhor preço <span className="text-brand-600">perto de você</span>.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-600 dark:text-slate-300">Consulte produtos, mercados e preços publicados na plataforma.</p><SearchBar className="mt-7 max-w-xl" placeholder="Busque produtos cadastrados" /><Button asChild size="lg" className="mt-7"><Link to="/dashboard">Acessar minha área <ArrowRight className="size-4" /></Link></Button></motion.div><motion.div initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: .12, duration: .45 }} className="mx-auto w-full max-w-md"><div className="rounded-3xl bg-brand-600 p-7 text-white shadow-2xl shadow-brand-600/25"><p className="text-sm font-semibold text-brand-100">QuantoTá</p><p className="mt-2 text-3xl font-black">Compare antes de comprar.</p><p className="mt-5 text-sm leading-6 text-brand-100">As ofertas e comparações exibidas abaixo são formadas a partir dos preços cadastrados por administradores e vendedores.</p></div></motion.div></div></section>; }
