import { ArrowRight, MapPin } from "lucide-react";
import { motion } from "framer-motion";
import { Link } from "react-router-dom";
import { Button } from "@/components/ui/Button";
import { SearchBar } from "@/components/ui/SearchBar";
export function Hero() { return <section className="overflow-hidden bg-gradient-to-br from-brand-50 via-white to-amber-50 py-14 dark:from-brand-500/10 dark:via-slate-950 dark:to-amber-500/5 sm:py-20"><div className="mx-auto grid max-w-7xl items-center gap-10 px-4 sm:px-6 lg:grid-cols-[1.1fr_.9fr] lg:px-8"><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .45 }}><span className="inline-flex items-center gap-1 rounded-full bg-brand-100 px-3 py-1 text-sm font-bold text-brand-700 dark:bg-brand-500/15 dark:text-brand-100"><MapPin className="size-4" /> Economia perto de você</span><h1 className="mt-5 max-w-2xl text-4xl font-black leading-tight tracking-tight sm:text-5xl">O melhor preço está <span className="text-brand-600">mais perto</span> do que você imagina.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-600 dark:text-slate-300">Compare preços nos seus mercados favoritos, acompanhe ofertas e economize em cada compra.</p><SearchBar className="mt-7 max-w-xl" placeholder="O que você quer economizar hoje?" /><div className="mt-4 flex items-center gap-3 text-sm text-slate-500"><span className="font-semibold text-brand-600">Mais buscados:</span> arroz, café, leite, detergente</div><Button asChild size="lg" className="mt-7"><Link to="/dashboard">Começar a economizar <ArrowRight className="size-4" /></Link></Button></motion.div><motion.div initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ delay: .12, duration: .45 }} className="relative mx-auto w-full max-w-md"><div className="rounded-3xl bg-brand-600 p-7 text-white shadow-2xl shadow-brand-600/25"><p className="text-sm font-semibold text-brand-100">Sua economia neste mês</p><p className="mt-2 text-5xl font-black">R$ 126,40</p><div className="mt-8 rounded-2xl bg-white/15 p-4 backdrop-blur"><div className="mb-3 flex justify-between text-sm"><span>Itens comparados</span><strong>42 produtos</strong></div><div className="h-2 overflow-hidden rounded-full bg-white/20"><div className="h-full w-3/4 rounded-full bg-amber-300" /></div></div></div><div className="absolute -bottom-5 -left-5 rounded-2xl bg-white p-4 shadow-xl dark:bg-slate-900"><p className="text-xs font-semibold text-slate-500">Oferta encontrada</p><p className="mt-1 font-black text-brand-600">-24% no café</p></div></motion.div></div></section>; }
